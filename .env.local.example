# .env.local.example - Example of local environment overrides
#
# Copy this file to .env.local and customize it with your local settings.
# Variables defined here will override those in .env
#
# .env.local is ignored by git, so it's safe to put sensitive data here
#
# Example overrides:

# Override n8n credentials for local development
# N8N_ENCRYPTION_KEY=my-local-encryption-key
# N8N_USER_MANAGEMENT_JWT_SECRET=my-local-jwt-secret

# Override Supabase credentials for local development
# POSTGRES_PASSWORD=my-local-postgres-password
# JWT_SECRET=my-local-jwt-secret-with-at-least-32-characters

# Override API URLs for local development
# API_EXTERNAL_URL=http://localhost:8000
# SITE_URL=http://localhost:3000
# SUPABASE_PUBLIC_URL=http://localhost:8000

# Override service hostnames for local testing
# N8N_HOSTNAME=n8n.local.test
# WEBUI_HOSTNAME=openwebui.local.test
# FLOWISE_HOSTNAME=flowise.local.test

# Add any other local overrides here...